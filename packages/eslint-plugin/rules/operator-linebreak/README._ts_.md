# ts/operator-linebreak

This rule extends the base [`operator-linebreak`](/rules/js/operator-linebreak) rule.
It adds support for TypeScript features that enforces a consistent linebreak style for operators.
